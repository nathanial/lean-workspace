<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{title}} - Homebase</title>
  <link rel="stylesheet" href="/css/app.css">
</head>
<body class="auth-container">
  <div class="auth-box">
    <!-- Brand mark -->
    <div style="text-align: center; margin-bottom: 1.5rem;">
      <div style="display: inline-flex; align-items: center; gap: 0.75rem;">
        <span style="width: 40px; height: 40px; display: flex; align-items: center; justify-content: center; background: var(--accent-coral); border: 2px solid var(--ink-black);">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
        </span>
        <span style="font-family: 'Bebas Neue', Impact, sans-serif; font-size: 2rem; letter-spacing: 0.08em; text-transform: uppercase; color: var(--paper-cream);">Homebase</span>
      </div>
    </div>

    {{#if flash.success}}
    <div class="flash flash-success">{{flash.success}}</div>
    {{/if}}
    {{#if flash.error}}
    <div class="flash flash-error">{{flash.error}}</div>
    {{/if}}
    {{#if flash.info}}
    <div class="flash flash-info">{{flash.info}}</div>
    {{/if}}

    {{{content}}}
  </div>
  <script src="/js/hot-reload.js"></script>
</body>
</html>
